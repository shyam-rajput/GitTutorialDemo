// Git Basic Commands:

// 1) git status : Status check karne ke liye.

// 2) git init : Create or initialize new git repository, git repository banane se ab yeh Git ki reposibility ho jaayegi ki bho track kare us folder ki files ko.

// 3) git add --a : Isse sabhi files staging area me chali jaayengi uske baad hame in files ko commit karna hoga.

// 4) git commit -m "First Commit"

// 5) git status : Status check karne ke liye.

// 6) git log : Agar aap jaanna chahte hai ki aapne kon kon se commit kiye hai tab aap git log ka use karnge. is command ko chlaane ke baad aap q press kar dena jisse aap isse exit ho jaao.

// 7) git add first.txt : Agar aapne kisi ek file ko modify kiya hai tab aap us file ko staging area me add karne ke liye is command ka use karenge, ek baar staging area me aa jaane ke baad aap us file ko commit kar sakte ho.

// 8) rm -rf .git  : Agar aap kisi ek folder ko track karna band karna chahte hai tab aap is command ko chala de, lekin yeh bahut danger command hai kyuki yeh aapka jo .git hidden folder hai usko delete kar dega, ab aapki git repository, git repository rahegi nahi.

// 9) git clone enter_repository_url : Isse Github se remote repository aapke local machin me aa jaayegi.

////////////////////////////////////////////

// Cloning a Remote Git Repository from GitHub:

// Step 1: copy the repository url from the Github

// Step 2: git clone enter_repository_url  : Isse Github se remote repository aapke local machin me aa jaayegi.
